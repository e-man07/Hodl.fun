# name: Test Suite

# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main, develop]

# env:
#   NODE_VERSION: '20'
#   PNPM_VERSION: '8'

# jobs:
#   # ============================================================================
#   # Unit Tests - Run in parallel for each service
#   # ============================================================================
#   unit-tests:
#     name: Unit Tests - ${{ matrix.service }}
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         service:
#           - common
#           - database
#           - redis
#           - api
#           - websocket
#           - indexer
#           - worker

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup pnpm
#         uses: pnpm/action-setup@v2
#         with:
#           version: ${{ env.PNPM_VERSION }}

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'pnpm'

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Generate Prisma client
#         run: pnpm prisma generate

#       - name: Run unit tests
#         run: |
#           if [[ "${{ matrix.service }}" == "common" ]]; then
#             pnpm --filter @hodlfun/common test
#           elif [[ "${{ matrix.service }}" == "database" ]]; then
#             pnpm --filter @hodlfun/database test
#           elif [[ "${{ matrix.service }}" == "redis" ]]; then
#             pnpm --filter @hodlfun/redis test
#           else
#             pnpm --filter ${{ matrix.service }} test
#           fi

#       - name: Upload coverage
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: coverage-unit-${{ matrix.service }}
#           path: coverage/
#           retention-days: 7

#   # ============================================================================
#   # Integration Tests - Require real database and Redis
#   # ============================================================================
#   integration-tests:
#     name: Integration Tests
#     runs-on: ubuntu-latest
#     needs: unit-tests

#     services:
#       postgres:
#         image: postgres:15
#         env:
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: hodlfun_test
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#       redis:
#         image: redis:7
#         ports:
#           - 6379:6379
#         options: >-
#           --health-cmd "redis-cli ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     env:
#       DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hodlfun_test
#       REDIS_URL: redis://localhost:6379

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup pnpm
#         uses: pnpm/action-setup@v2
#         with:
#           version: ${{ env.PNPM_VERSION }}

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'pnpm'

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Generate Prisma client
#         run: pnpm prisma generate

#       - name: Run database migrations
#         run: pnpm prisma migrate deploy

#       - name: Run integration tests
#         run: pnpm test:integration

#       - name: Upload coverage
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: coverage-integration
#           path: coverage/
#           retention-days: 7

#   # ============================================================================
#   # E2E Tests - Full system tests
#   # ============================================================================
#   e2e-tests:
#     name: E2E Tests
#     runs-on: ubuntu-latest
#     needs: integration-tests

#     services:
#       postgres:
#         image: postgres:15
#         env:
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: hodlfun_test
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#       redis:
#         image: redis:7
#         ports:
#           - 6379:6379
#         options: >-
#           --health-cmd "redis-cli ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     env:
#       DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hodlfun_test
#       REDIS_URL: redis://localhost:6379
#       JWT_SECRET: test-jwt-secret-for-ci
#       JWT_REFRESH_SECRET: test-jwt-refresh-secret-for-ci

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup pnpm
#         uses: pnpm/action-setup@v2
#         with:
#           version: ${{ env.PNPM_VERSION }}

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'pnpm'

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Generate Prisma client
#         run: pnpm prisma generate

#       - name: Run database migrations
#         run: pnpm prisma migrate deploy

#       - name: Run E2E tests
#         run: pnpm test:e2e

#       - name: Upload coverage
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: coverage-e2e
#           path: coverage/
#           retention-days: 7

#   # ============================================================================
#   # Coverage Report - Merge all coverage reports
#   # ============================================================================
#   coverage-report:
#     name: Coverage Report
#     runs-on: ubuntu-latest
#     needs: [unit-tests, integration-tests, e2e-tests]
#     if: always()

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Download all coverage artifacts
#         uses: actions/download-artifact@v4
#         with:
#           pattern: coverage-*
#           path: coverage-reports/
#           merge-multiple: true

#       - name: Display coverage summary
#         run: |
#           echo "## Test Coverage Summary" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "| Category | Status |" >> $GITHUB_STEP_SUMMARY
#           echo "|----------|--------|" >> $GITHUB_STEP_SUMMARY
#           echo "| Unit Tests | ${{ needs.unit-tests.result }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| Integration Tests | ${{ needs.integration-tests.result }} |" >> $GITHUB_STEP_SUMMARY
#           echo "| E2E Tests | ${{ needs.e2e-tests.result }} |" >> $GITHUB_STEP_SUMMARY

#   # ============================================================================
#   # Final Status Check
#   # ============================================================================
#   test-status:
#     name: Test Status
#     runs-on: ubuntu-latest
#     needs: [unit-tests, integration-tests, e2e-tests]
#     if: always()

#     steps:
#       - name: Check test results
#         run: |
#           if [[ "${{ needs.unit-tests.result }}" == "failure" ]] || \
#              [[ "${{ needs.integration-tests.result }}" == "failure" ]] || \
#              [[ "${{ needs.e2e-tests.result }}" == "failure" ]]; then
#             echo "One or more test jobs failed"
#             exit 1
#           fi
#           echo "All tests passed successfully!"
